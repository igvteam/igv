sudo: true
language: java
cache: false
#before_cache:
#  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
#  - rm -rf $HOME/.gradle/caches/*/plugin-resolution/
#cache:
#  directories:
#    - $HOME/.gradle/caches/
#    - $HOME/.gradle/wrapper/
jdk:
  - openjdk8
  - openjdk11
install:
- rm -rf $HOME/.gradle
- sudo ./gradlew clean
- sudo ./gradlew assemble
# - ./gradlew -b build_java11.gradle assemble
script:
- sudo ./gradlew -b build.gradle build
# - jdk_switcher use openjdk11
# - ./gradlew -b build_java11.gradle check
